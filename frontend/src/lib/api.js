/**
 * URL base de la API — único lugar donde se referencia REACT_APP_BACKEND_URL.
 * Importar `API` desde aquí en lugar de construirlo en cada archivo.
 */
export const API = `${process.env.REACT_APP_BACKEND_URL}/api`;
